---
apiVersion: v1
kind: Service
metadata:
  name: affine-svc
  namespace: apps
spec:
  type: LoadBalancer
  ports:
    - port: 3010
      protocol: TCP
      targetPort: 3010
  selector:
    app.kubernetes.io/name: affine-svc


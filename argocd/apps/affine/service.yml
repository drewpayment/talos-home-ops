---
apiVersion: v1
kind: Service
metadata:
  name: affine
  namespace: apps
  labels:
    app.kubernetes.io/name: affine
spec:
  type: ClusterIP
  ports:
    - port: 3010
      protocol: TCP
      targetPort: 3010
      name: http
  selector:
    app.kubernetes.io/name: affine

